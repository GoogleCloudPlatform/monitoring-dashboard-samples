alert_policy_templates:
  - id: "database-size-too-large"
    display_name: "PostgreSQL - Prometheus - Database Size Too Large"
    description: "If the database is growing larger than expected (100 GB by default), it may signal excess data being stored or excess records not being properly removed by an application."
    version: 1
