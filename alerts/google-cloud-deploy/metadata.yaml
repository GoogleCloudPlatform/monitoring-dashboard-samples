alert_policy_templates:
-
  id: release-render-failure-within-pipeline
  description: "Get notified when a release render fails within this delivery pipeline."
  version: 1
-
  id: rollout-failure-within-pipeline
  description: "Get notified when a rollout fails within this delivery pipeline."
  version: 1
-
  id: rollout-canary-advance-required-within-pipeline
  description: "Get notified when a canary advance is required for a rollout within this delivery pipeline."
  version: 1
-
  id: rollout-approval-required-within-pipeline
  description: "Get notified when a rollout approval is required within this delivery pipeline."
  version: 1

