id: ingress-nginx
short_name: Ingress NGINX
display_name: Ingress NGINX Controller
description: The Ingress NGINX Controller is an Ingress controller for 
  Kubernetes using NGINX as a reverse proxy and load balancer. It is built 
  around the Kubernetes Ingress resource, and uses a ConfigMap to store the 
  controller configuration.
