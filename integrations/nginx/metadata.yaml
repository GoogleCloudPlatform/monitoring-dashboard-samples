id: nginx
display_name: NGINX
short_name: NGINX
description: >-
  The Nginx integration collects connection metrics and access logs.
  Connection metrics are dimensioned by their current state (active, reading,
  waiting, etc). Access logs are parsed for their connection details, which
  include fields mapped to request, client, server, and message.

